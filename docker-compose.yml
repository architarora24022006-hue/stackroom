version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: ragqa-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ragqa
      POSTGRES_USER: ragqa
      POSTGRES_PASSWORD: ragqa
    ports:
      - "5432:5432"
    volumes:
      - ragqa_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ragqa -d ragqa"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  ragqa_db_data:
